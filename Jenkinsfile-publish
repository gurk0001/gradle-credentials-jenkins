pipeline {
    agent any

    environment {
        ARCHIVA = credentials('popye-archiva')
    }
    stages {
        stage('Publish') {
            steps {
                git 'https://github.com/gurk0001/gradle-credentials-jenkins.git'

                sh './gradlew publish --info --stacktrace'
            }
        }
    }
}